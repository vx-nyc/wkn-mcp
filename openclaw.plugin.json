{
  "id": "vx-memory",
  "name": "VX Memory",
  "description": "VX memory for OpenClaw. Connects to the hosted MCP endpoint over HTTP with OAuth — no static API key needed.",
  "version": "1.0.0",
  "skills": [
    "skills/openclaw/vx-memory"
  ],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiBaseUrl": {
        "type": "string",
        "default": "https://api.onememory.co/mcp",
        "description": "VX MCP HTTP endpoint. The OpenClaw runtime handles the OAuth flow on first use."
      },
      "source": {
        "type": "string",
        "default": "openclaw",
        "description": "Source tag stored with VX memories."
      },
      "name": {
        "type": "string",
        "default": "VX",
        "description": "Friendly display name used in VX responses."
      }
    }
  },
  "uiHints": {
    "apiBaseUrl": {
      "label": "VX MCP URL",
      "placeholder": "https://api.onememory.co/mcp"
    },
    "source": {
      "label": "Source Tag",
      "placeholder": "openclaw"
    },
    "name": {
      "label": "Display Name",
      "placeholder": "VX"
    }
  }
}
