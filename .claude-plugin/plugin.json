{
  "name": "vx-mcp",
  "version": "1.0.0",
  "description": "VX Universal memory for Claude Code. Connects to the hosted MCP endpoint over HTTP with OAuth — no API key needed.",
  "mcpServers": {
    "vx": {
      "type": "http",
      "url": "https://api.onememory.co/mcp"
    }
  }
}
